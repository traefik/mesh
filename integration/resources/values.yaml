## Default values for maesh
image:
  name: containous/maesh
  # (Optional)
  pullPolicy: IfNotPresent
  # (Optional)
  tag: latest
  # (Optional)
  # pullSecret: xxx
  traefik: traefik:v2.0.0-beta1

debug: true

smi: false

tracing:
  enabled: true
metrics:
  enabled: true
