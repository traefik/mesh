## Default values for i3o
image:
  name: containous/i3o
  # (Optional)
  pullPolicy: IfNotPresent
  # (Optional)
  tag: latest
  # (Optional)
  # pullSecret: xxx
  traefik: containous/traefik:experimental-v2.0

debug: true

smi: false

tracing:
  enabled: false
