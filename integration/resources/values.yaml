## Default values for i3o
image:
  name: containous/i3o
  # (Optional)
  pullPolicy: IfNotPresent
  # (Optional)
  tag: latest
  # (Optional)
  # pullSecret: xxx
  traefik: traefik:v2.0.0-beta1

debug: true

smi: false

tracing:
  enabled: false
